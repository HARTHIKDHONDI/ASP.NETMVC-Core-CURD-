@model IEnumerable<Basic_model.Models.Order_date_details>

@{
    ViewData["Title"] = "od_date";
    Layout = "~/Views/Shared/Index_Layout.cshtml";
}

<h1>ORDER DATE</h1>


<section class="ftco-section border border-primary border-5  rounded gap-5 p-5">
    <div class="container">
        <div class="row justify-content-center ">
            <div class="col-md-6 text-center mb-5">
                <h2 class="heading-section">OrderDate with search</h2>

            </div>
        </div>

        @using (Html.BeginForm("od_date","Register",FormMethod.Get))
        {
            <table style="width:100%">
                <tr>
                    <td>
                        <div class="d-flex flex-row align-items-center mb-2">

                            <div class="form-outline flex-fill mb-0">
                                <input type="date" name="start" />


                                <label class="form-label" for="form3Example3c"><b>From Date</b></label>

                            </div>
                        </div>
                        <div class="d-flex flex-row align-items-center mb-2">

                            <div class="form-outline flex-fill mb-0">
                                <input type="date" name="end" />

                                <label class="form-label" for="form3Example3c"><b>To Date</b></label>

                            </div>
                        </div>
                        <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-2">
                            <input type="submit" name="submit" id="submit" class="btn btn-primary btn-lg">
                        </div>
                    </td>
                </tr>
            </table>
        }
            @*if (ViewBag.details== null)
        {
        <table style="width:100%">
        <tr>
        <td>
        <div class="d-flex flex-row align-items-center mb-2">

        <div class="form-outline flex-fill mb-0">
        <input type="date" name="start" />


        <label class="form-label" for="form3Example3c"><b>From Date</b></label>

        </div>
        </div>
        <div class="d-flex flex-row align-items-center mb-2">

        <div class="form-outline flex-fill mb-0">
        <input type="date" name="end" />

        <label class="form-label" for="form3Example3c"><b>To Date</b></label>

        </div>
        </div>
        <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-2">
        <input type="submit" name="submit" id="submit" class="btn btn-primary btn-lg">

        </div>
        </td>
        </tr>
        </table>
        }
        else
        {*@
            <div class="row ">
                    <div class="col">
                        <div class="table-wrap d-flex justify-content-center align-content-center ">
                         

                            <table class="table table-striped  " id="myTable">
                                <thead>
                                    <tr style="background-color:royalblue;">
                                        <th>CustomerID</th>
                                        <th>OrderDate</th>
                                        <th>Freight</th>
                                        <th>ShipName</th>
                                        <th>Ship Address</th>
                                    </tr>

                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.CustomerID</td>
                                            <td>@item.OrderDate</td>
                                            <td>@item.Freight</td>
                                            <td>@item.ShipName</td>
                                            <td>@item.ShipAddress</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
            </div>
            
            
        
    </div>
</section>